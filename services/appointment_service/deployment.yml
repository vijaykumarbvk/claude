apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: hospital-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      containers:
      - name: appointment-service
        image: 286593018001.dkr.ecr.us-east-1.amazonaws.com/venus-appointment-service:3d44c8e049fd340c3881faf3ecb928815dce903c
        ports:
        - containerPort: 5004
        env:
        - name: DB_HOST
          value: "mysql-service"
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: "hospital_db"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: hospital-ns
spec:
  selector:
    app: appointment-service
  ports:
  - port: 5004
    targetPort: 5004
  type: ClusterIP
